#!/usr/bin/env node

require('dotenv').config()
const app = require('../index').default

const PORT = process.env.PORT || 3000

app.listen(PORT, () => {
  console.log(`slackpub running on port ${PORT}`)
})
